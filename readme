npm init -y (init project)
create index.js

npm install express --save (--save = update express in package.json)
npm install body-parser --save
npm install (install everything in package.json)

// run script
node index.js
// run script with auto restart  
nodemon index.js

// clear port
lsof -i:3000
kill -9 *PID*